'use client'

import { useMemo, useState } from 'react'
import { Button } from '@/components/ui/button'
import { Bell, Building2, ChevronDown, ChevronLeft, ChevronRight, CircleHelp, Clock3, LayoutDashboard, LogOut, Menu, MoreHorizontal, Plus, Search, Settings, ShieldCheck, SlidersHorizontal, TrendingUp, UserRound, Users, X } from 'lucide-react'
import { IClient } from './types/client'


const clients: IClient[] = [
  { id: 1, name: 'John Smith', company: 'Acme Inc.', email: 'john@acme.com', phone: '+1 (555) 012-3456', status: 'Won', created: 'Jun 14, 2024', initials: 'JS' },
  { id: 2, name: 'Alex Johnson', company: 'Vertex Labs', email: 'alex@vertexlabs.com', phone: '+1 (555) 222-9081', status: 'In Progress', created: 'Jun 12, 2024', initials: 'AJ' },
  { id: 3, name: 'Sarah Wilson', company: 'Northstar Co.', email: 'sarah@northstar.co', phone: '+1 (555) 338-1842', status: 'New', created: 'Jun 10, 2024', initials: 'SW' },
  { id: 4, name: 'Michael Brown', company: 'Orbit Systems', email: 'michael@orbit.io', phone: '+1 (555) 441-7620', status: 'Lost', created: 'Jun 08, 2024', initials: 'MB' },
  { id: 5, name: 'David Miller', company: 'Pioneer Works', email: 'david@pioneer.works', phone: '+1 (555) 515-6240', status: 'Won', created: 'Jun 05, 2024', initials: 'DM' },
]
const users: ITeamUser[] = [
  { id: 1, name: 'John Smith', email: 'john@acme.com', role: 'Admin', status: 'Active', joined: 'Jan 12, 2024', initials: 'JS' },
  { id: 2, name: 'Emily Davis', email: 'emily@acme.com', role: 'Manager', status: 'Active', joined: 'Feb 24, 2024', initials: 'ED' },
  { id: 3, name: 'Chris Lee', email: 'chris@acme.com', role: 'Manager', status: 'Inactive', joined: 'Mar 08, 2024', initials: 'CL' },
]
const statusClass: Record<string, string> = { New: 'bg-blue-50 text-blue-700', 'In Progress': 'bg-amber-50 text-amber-700', Won: 'bg-emerald-50 text-emerald-700', Lost: 'bg-rose-50 text-rose-700', Active: 'bg-emerald-50 text-emerald-700', Inactive: 'bg-slate-100 text-slate-600' }

function StatusBadge({ status }: { status: string }) { return <span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-medium ${statusClass[status]}`}>{status}</span> }
function Avatar({ initials, small = false }: { initials: string; small?: boolean }) { return <span className={`inline-flex shrink-0 items-center justify-center rounded-full bg-slate-100 font-semibold text-slate-600 ${small ? 'size-8 text-[11px]' : 'size-9 text-xs'}`}>{initials}</span> }
function StatCard({ title, value, detail, icon: Icon, tone }: { title: string; value: string; detail: string; icon: typeof Users; tone: string }) { return <div className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><div><p className="text-sm font-medium text-slate-500">{title}</p><p className="mt-2 text-3xl font-semibold tracking-tight text-slate-950">{value}</p></div><span className={`flex size-10 items-center justify-center rounded-lg ${tone}`}><Icon className="size-5" /></span></div><p className="mt-4 text-xs text-slate-500"><span className="font-semibold text-emerald-600">{detail.split(' ')[0]} {detail.split(' ')[1]}</span> {detail.split(' ').slice(2).join(' ')}</p></div> }

export default function Page() {
  const [view, setView] = useState<'Dashboard' | 'Clients' | 'Users' | 'Profile'>('Dashboard')
  const [mobileNav, setMobileNav] = useState(false)
  const [query, setQuery] = useState('')
  const [dialog, setDialog] = useState<'client' | 'user' | 'profile' | null>(null)
  const filtered = useMemo(() => clients.filter((client) => `${client.name} ${client.company} ${client.email}`.toLowerCase().includes(query.toLowerCase())), [query])
  const nav = [{ label: 'Dashboard', icon: LayoutDashboard }, { label: 'Clients', icon: Users }, { label: 'Users', icon: ShieldCheck }, { label: 'Profile', icon: UserRound }]
  return <div className="min-h-screen bg-[#f7f8fa] text-slate-900">
    {mobileNav && <div className="fixed inset-0 z-40 bg-slate-950/20 lg:hidden" onClick={() => setMobileNav(false)} />}
    <aside className={`fixed inset-y-0 left-0 z-50 flex w-64 flex-col border-r border-slate-200 bg-white px-4 py-5 transition-transform lg:translate-x-0 ${mobileNav ? 'translate-x-0' : '-translate-x-full'}`}>
      <div className="flex items-center justify-between px-3"><div className="flex items-center gap-2.5"><span className="flex size-9 items-center justify-center rounded-lg bg-slate-900 text-white"><Building2 className="size-4" /></span><span className="text-lg font-semibold tracking-tight">Northstar</span></div><button className="lg:hidden" onClick={() => setMobileNav(false)} aria-label="Close navigation"><X className="size-5" /></button></div>
      <div className="mt-10 px-3 text-[11px] font-semibold uppercase tracking-wider text-slate-400">Workspace</div>
      <nav className="mt-3 space-y-1">{nav.slice(0, 2).map(({ label, icon: Icon }) => <button key={label} onClick={() => { setView(label as typeof view); setMobileNav(false) }} className={`flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium transition ${view === label ? 'bg-slate-900 text-white' : 'text-slate-600 hover:bg-slate-50'}`}><Icon className="size-[18px]" />{label}</button>)}</nav>
      <div className="mt-8 px-3 text-[11px] font-semibold uppercase tracking-wider text-slate-400">Administration</div><nav className="mt-3"><button onClick={() => setView('Users')} className={`flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium ${view === 'Users' ? 'bg-slate-900 text-white' : 'text-slate-600 hover:bg-slate-50'}`}><ShieldCheck className="size-[18px]" />Users</button></nav>
      <div className="mt-8 px-3 text-[11px] font-semibold uppercase tracking-wider text-slate-400">Account</div><nav className="mt-3"><button onClick={() => setView('Profile')} className={`flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium ${view === 'Profile' ? 'bg-slate-900 text-white' : 'text-slate-600 hover:bg-slate-50'}`}><UserRound className="size-[18px]" />Profile</button></nav>
      <div className="mt-auto border-t border-slate-100 pt-4"><div className="flex items-center gap-3 px-3 py-2"><Avatar initials="JS" small /><div className="min-w-0"><p className="truncate text-sm font-medium">John Smith</p><p className="text-xs text-slate-500">Manager</p></div><button className="ml-auto text-slate-400" aria-label="More user options"><MoreHorizontal className="size-4" /></button></div><button className="mt-2 flex w-full items-center gap-3 px-3 py-2 text-sm text-slate-500 hover:text-slate-900"><LogOut className="size-4" />Log out</button></div>
    </aside>
    <main className="lg:pl-64"><header className="sticky top-0 z-30 flex h-16 items-center justify-between border-b border-slate-200 bg-white/90 px-5 backdrop-blur md:px-8"><div className="flex items-center gap-3"><button className="lg:hidden" onClick={() => setMobileNav(true)} aria-label="Open navigation"><Menu className="size-5" /></button><div><h1 className="text-base font-semibold">{view}</h1><p className="hidden text-xs text-slate-500 sm:block">{view === 'Dashboard' ? 'Overview of your CRM activity' : view === 'Clients' ? 'Manage and track your clients' : view === 'Users' ? 'Manage team members' : 'Manage your account'}</p></div></div><div className="flex items-center gap-3"><button className="relative rounded-lg p-2 text-slate-500 hover:bg-slate-100" aria-label="Notifications"><Bell className="size-[18px]" /><span className="absolute right-1.5 top-1.5 size-1.5 rounded-full bg-blue-600" /></button><div className="hidden h-6 w-px bg-slate-200 sm:block" /><button className="flex items-center gap-2"><Avatar initials="JS" small /><ChevronDown className="hidden size-4 text-slate-400 sm:block" /></button></div></header>
      <div className="mx-auto max-w-[1440px] p-5 md:p-8">{view === 'Dashboard' && <Dashboard onAdd={() => setDialog('client')} />}{view === 'Clients' && <ClientsPage query={query} setQuery={setQuery} clients={filtered} onAdd={() => setDialog('client')} />}{view === 'Users' && <UsersPage onAdd={() => setDialog('user')} />}{view === 'Profile' && <Profile onEdit={() => setDialog('profile')} />}</div></main>
    {dialog && <Dialog kind={dialog} onClose={() => setDialog(null)} />}
  </div>
}

function Dashboard({ onAdd }: { onAdd: () => void }) { return <div className="space-y-7"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm text-slate-500">Monday, June 17, 2024</p><h2 className="mt-1 text-2xl font-semibold tracking-tight">Good morning, John</h2></div><select className="h-10 rounded-lg border border-slate-200 bg-white px-3 text-sm text-slate-600 outline-none"><option>Last 30 days</option><option>Last 7 days</option><option>All time</option></select></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard title="Total Clients" value="128" detail="+12% from last month" icon={Users} tone="bg-blue-50 text-blue-600" /><StatCard title="New Clients" value="24" detail="+8% from last month" icon={TrendingUp} tone="bg-indigo-50 text-indigo-600" /><StatCard title="In Progress" value="42" detail="+6% from last month" icon={Clock3} tone="bg-amber-50 text-amber-600" /><StatCard title="Won" value="52" detail="+14% from last month" icon={ShieldCheck} tone="bg-emerald-50 text-emerald-600" /></div><div className="grid gap-5 xl:grid-cols-[1.5fr_1fr]"><div className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><h3 className="font-semibold">New Clients</h3><p className="mt-1 text-xs text-slate-500">Client acquisition over time</p></div><button className="text-slate-400"><MoreHorizontal className="size-5" /></button></div><div className="mt-6 h-52"><div className="flex h-full items-end gap-2 border-b border-l border-slate-100 px-3 pb-0 pt-4">{[35, 48, 42, 62, 54, 76, 68, 88, 72, 96, 84, 100].map((height, index) => <div key={index} className="group flex h-full flex-1 items-end"><div className="w-full rounded-t-sm bg-slate-200 transition group-hover:bg-slate-900" style={{ height: `${height}%` }} /></div>)}</div><div className="flex justify-between pl-4 pt-2 text-[10px] text-slate-400"><span>May 20</span><span>May 27</span><span>Jun 03</span><span>Jun 10</span><span>Jun 17</span></div></div></div><div className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="font-semibold">Clients by Status</h3><p className="mt-1 text-xs text-slate-500">Current pipeline breakdown</p><div className="mt-7 flex items-center gap-6"><div className="relative flex size-36 shrink-0 items-center justify-center rounded-full" style={{ background: 'conic-gradient(#10b981 0 41%, #f59e0b 41% 74%, #3b82f6 74% 89%, #f43f5e 89% 100%)' }}><div className="flex size-24 items-center justify-center rounded-full bg-white"><div className="text-center"><p className="text-2xl font-semibold">128</p><p className="text-[10px] text-slate-500">Total</p></div></div></div><div className="space-y-3 text-xs">{[['Won', '52', 'bg-emerald-500'], ['In Progress', '42', 'bg-amber-500'], ['New', '24', 'bg-blue-500'], ['Lost', '10', 'bg-rose-500']].map(([label, value, dot]) => <div key={label} className="flex items-center gap-2"><span className={`size-2 rounded-full ${dot}`} /><span className="w-20 text-slate-500">{label}</span><b>{value}</b></div>)}</div></div></div></div><div className="rounded-xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-slate-100 p-5"><div><h3 className="font-semibold">Recent Clients</h3><p className="mt-1 text-xs text-slate-500">Your most recently added clients</p></div><Button variant="outline" size="sm" onClick={onAdd}><Plus className="mr-2 size-4" />Add client</Button></div><ClientTable clients={clients.slice(0, 4)} compact /></div></div> }

function ClientsPage({ clients, query, setQuery, onAdd }: { clients: Client[]; query: string; setQuery: (v: string) => void; onAdd: () => void }) { return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h2 className="text-2xl font-semibold tracking-tight">Clients</h2><p className="mt-1 text-sm text-slate-500">Manage and track your clients</p></div><Button onClick={onAdd}><Plus className="mr-2 size-4" />Add client</Button></div><div className="flex flex-col gap-3 rounded-xl border border-slate-200 bg-white p-3 shadow-sm sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-2.5 size-4 text-slate-400" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search clients..." className="h-9 w-full rounded-lg border-0 bg-slate-50 pl-9 pr-3 text-sm outline-none ring-1 ring-slate-200 focus:ring-slate-400" /></div><select className="h-9 rounded-lg border border-slate-200 bg-white px-3 text-sm text-slate-600"><option>All statuses</option><option>New</option><option>In Progress</option><option>Won</option></select><Button variant="outline" size="sm"><SlidersHorizontal className="mr-2 size-4" />Filters</Button></div><div className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><ClientTable clients={clients} /><div className="flex items-center justify-between border-t border-slate-100 p-4 text-xs text-slate-500"><span>Showing {clients.length} of 128 clients</span><div className="flex gap-1"><button className="rounded border border-slate-200 p-1.5"><ChevronLeft className="size-4" /></button><button className="rounded border border-slate-900 bg-slate-900 p-1.5 text-white">1</button><button className="rounded border border-slate-200 px-2.5 py-1.5">2</button><button className="rounded border border-slate-200 px-2.5 py-1.5">3</button><button className="rounded border border-slate-200 p-1.5"><ChevronRight className="size-4" /></button></div></div></div></div> }
function ClientTable({ clients, compact = false }: { clients: Client[]; compact?: boolean }) { return <div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-slate-50 text-xs text-slate-500"><tr>{['Client', 'Company', 'Email', 'Phone', 'Status', 'Created', ''].map((h) => <th key={h} className="px-5 py-3 font-medium">{h}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{clients.map((client) => <tr key={client.id} className="hover:bg-slate-50/70"><td className="px-5 py-3.5"><div className="flex items-center gap-3"><Avatar initials={client.initials} /><div><p className="font-medium text-slate-800">{client.name}</p><p className="text-xs text-slate-500">{client.email}</p></div></div></td><td className="px-5 py-3.5 text-slate-600">{client.company}</td><td className="px-5 py-3.5 text-slate-500">{client.email}</td><td className="px-5 py-3.5 text-slate-500">{client.phone}</td><td className="px-5 py-3.5"><StatusBadge status={client.status} /></td><td className="px-5 py-3.5 text-slate-500">{client.created}</td><td className="px-5 py-3.5"><button className="rounded p-1.5 text-slate-400 hover:bg-slate-100" aria-label={`Actions for ${client.name}`}><MoreHorizontal className="size-4" /></button></td></tr>)}</tbody></table></div> }
function UsersPage({ onAdd }: { onAdd: () => void }) { return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h2 className="text-2xl font-semibold tracking-tight">Users</h2><p className="mt-1 text-sm text-slate-500">Manage team members</p></div><Button onClick={onAdd}><Plus className="mr-2 size-4" />Add user</Button></div><div className="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-slate-50 text-xs text-slate-500"><tr>{['User', 'Email', 'Role', 'Status', 'Joined', ''].map((h) => <th key={h} className="px-5 py-3 font-medium">{h}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{users.map((user) => <tr key={user.id}><td className="px-5 py-4"><div className="flex items-center gap-3"><Avatar initials={user.initials} /><span className="font-medium">{user.name}</span></div></td><td className="px-5 py-4 text-slate-500">{user.email}</td><td className="px-5 py-4"><span className="text-slate-600">{user.role}</span></td><td className="px-5 py-4"><StatusBadge status={user.status} /></td><td className="px-5 py-4 text-slate-500">{user.joined}</td><td className="px-5 py-4"><button className="rounded p-1.5 text-slate-400 hover:bg-slate-100"><MoreHorizontal className="size-4" /></button></td></tr>)}</tbody></table></div></div></div> }
function Profile({ onEdit }: { onEdit: () => void }) { return <div className="max-w-3xl space-y-6"><div><h2 className="text-2xl font-semibold tracking-tight">Profile</h2><p className="mt-1 text-sm text-slate-500">Manage your personal information and preferences</p></div><div className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-col gap-5 sm:flex-row sm:items-center"><Avatar initials="JS" /><div className="flex-1"><h3 className="text-lg font-semibold">John Smith</h3><p className="text-sm text-slate-500">john@acme.com</p><StatusBadge status="Manager" /></div><Button variant="outline" onClick={onEdit}>Edit profile</Button></div><div className="mt-7 border-t border-slate-100 pt-6"><h3 className="font-semibold">Account information</h3><div className="mt-4 grid gap-5 sm:grid-cols-3"><div><p className="text-xs text-slate-500">Display name</p><p className="mt-1 text-sm font-medium">John Smith</p></div><div><p className="text-xs text-slate-500">Email</p><p className="mt-1 text-sm font-medium">john@acme.com</p></div><div><p className="text-xs text-slate-500">Role</p><p className="mt-1 text-sm font-medium">Manager</p></div></div></div></div><div className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><h3 className="font-semibold">Security</h3><p className="mt-1 text-sm text-slate-500">Manage your password and active sessions.</p><div className="mt-5 flex flex-col gap-3 sm:flex-row"><Button variant="outline">Change password</Button><Button variant="ghost" className="text-rose-600 hover:text-rose-700">Log out</Button></div></div></div> }
function Dialog({ kind, onClose }: { kind: 'client' | 'user' | 'profile'; onClose: () => void }) { const title = kind === 'client' ? 'Add client' : kind === 'user' ? 'Add user' : 'Edit profile'; return <div className="fixed inset-0 z-[60] flex items-center justify-center bg-slate-950/30 p-4" role="dialog" aria-modal="true"><div className="w-full max-w-lg rounded-xl border border-slate-200 bg-white p-6 shadow-xl"><div className="flex items-start justify-between"><div><h2 className="text-lg font-semibold">{title}</h2><p className="mt-1 text-sm text-slate-500">Update the details below. Changes are for preview only.</p></div><button onClick={onClose} aria-label="Close dialog" className="rounded-md p-1 text-slate-400 hover:bg-slate-100"><X className="size-5" /></button></div><div className="mt-6 grid gap-4 sm:grid-cols-2">{(kind === 'user' ? ['Full name', 'Email', 'Role', 'Status'] : ['Name', 'Email', 'Phone', 'Company', 'Status']).map((field) => <label key={field} className="grid gap-1.5 text-sm font-medium text-slate-700 sm:col-span-1">{field}<input placeholder={`Enter ${field.toLowerCase()}`} className="h-10 rounded-lg border border-slate-200 px-3 font-normal outline-none focus:border-slate-400" />{field === 'Status' && <select className="-mt-11 h-10 w-full bg-transparent px-3 text-sm outline-none"><option>New</option><option>In Progress</option><option>Won</option></select>}</label>)}</div><div className="mt-7 flex justify-end gap-3"><Button variant="outline" onClick={onClose}>Cancel</Button><Button onClick={onClose}>{kind === 'profile' ? 'Save changes' : kind === 'user' ? 'Create user' : 'Create client'}</Button></div></div></div> }
